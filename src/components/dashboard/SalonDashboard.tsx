'use client'

import { ProviderDashboard } from './ProviderDashboard'

// For now, salon dashboard is similar to provider dashboard
// but can be customized later for multi-provider management
export function SalonDashboard() {
  return <ProviderDashboard />
}